{
  "name": "port-authority",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "build": "node --check server.mjs && node --check processes.mjs && node --check memory.mjs && node --check public/app.js && node --check public/processes.js",
    "test": "node --test processes.test.mjs quick-quit.test.mjs memory.test.mjs"
  },
  "type": "module"
}
