{
  "name": "port-authority",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "build": "node --check server.mjs && node --check public/app.js"
  },
  "type": "module"
}
